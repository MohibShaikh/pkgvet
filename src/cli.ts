#!/usr/bin/env node
// pkgcheck CLI entry point (scaffold).
//
// Real argument parsing and analysis-engine wiring land during implementation,
// following the approved design. Kept intentionally minimal so the repo structure
// is legible without pre-empting the implementation plan / TDD.

console.error("pkgcheck: not implemented yet (scaffold).");
process.exit(2);
